<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.armylife.payments.mapper.RedEnvelopesMapper">
    <resultMap id="BaseResultMap" type="cn.armylife.payments.domain.RedEnvelopes">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="red_id" jdbcType="BIGINT" property="redId" />
        <result column="amount_number" jdbcType="DECIMAL" property="amountNumber" />
        <result column="user_id" jdbcType="BIGINT" property="userId" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="creat_time" jdbcType="VARCHAR" property="creatTime" />
        <result column="end_time" jdbcType="VARCHAR" property="endTime" />
    </resultMap>

    <insert id="insert" parameterType="cn.armylife.payments.domain.RedEnvelopes">
        <!--
    WARNING - @mbggenerated
    This element is automatically generated by MyBatis Generator, do not modify.
  -->
        <selectKey keyProperty="redId" order="AFTER" resultType="java.lang.Long">
        SELECT LAST_INSERT_ID()
    </selectKey>
        insert into red_envelopes ( amount_number, user_id,
        status,creat_time
        )
        values ( #{amountNumber,jdbcType=DECIMAL}, #{userId,jdbcType=BIGINT},
        #{status,jdbcType=INTEGER},
        #{creatTime,jdbcType=VARCHAR}
        )
    </insert>

    <update id="update">
    update red_envelopes
    <set>
        <if test="amountNumber!=null">
            amount_number=#{amountNumber},
        </if>
        <if test="user_id!=null">
            user_id=#{userId},
        </if>
        <if test="status!=null">
            status=#{status},
        </if>
    </set>
    1=1 where red_id=#{redId}
        and <![CDATA[  TO_DAYS(NOW()) - TO_DAYS(creat_time) <= 30]]>
    </update>

    <select id="selectRedForId" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select * from red_envelopes where red_id=#{redId} and status=0
        and  <![CDATA[ TO_DAYS(NOW()) - TO_DAYS(creat_time) <= 30]]>
    </select>
    <select id="selectRedForUser" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select * from
        red_envelopes
        where
        user_id=#{userId}
        and
        status=0
        and
       <![CDATA[  TO_DAYS(NOW()) - TO_DAYS(creat_time) <= 30]]>
    </select>
    <!--    将红包状态更改已使用-->
    <update id="updateRedStatus">
        update red_envelopes set status=1 where red_id=#{redId} and
        and <![CDATA[  TO_DAYS(NOW()) - TO_DAYS(creat_time) <= 30]]>
    </update>
</mapper>